# Mixed negatives config for Flickr30k
# Usage: khoji multimodal flickr30k_mixed.yaml
model:
  name: openai/clip-vit-base-patch32
  adapter_path: null
  dtype: null
  lora_target: both

data:
  dataset: nlphuji/flickr30k
  split: train
  negatives: mixed
  n_random: 2
  n_hard: 1
  n_queries: null
  corpus_size: null
  top_k: 50
  skip_top: 0
  mining_rounds: 1
  cache_dir: null

lora:
  r: 16
  alpha: 32
  dropout: 0.1
  target_modules: null

train:
  epochs: 3
  batch_size: 32
  grad_accum_steps: 1
  lr: 2e-5
  weight_decay: 0.01
  warmup_steps: 100
  max_grad_norm: 1.0
  max_length: 77
  loss: infonce
  margin: 0.2
  temperature: 0.05
  mixed_precision: bf16
  overfit_batches: null
  sanity_check_samples: 10
  save_every_n_steps: null
  keep_all_checkpoints: false

seed: 42

eval:
  dataset: null
  k_values: [1, 5, 10]
  split: test
  n_queries: null
  corpus_size: null
  run_before: true
  run_after: true

output_dir: ./forge-output/flickr30k-mixed
